<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
       http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-2.5.xsd">

    <!--
        Configure here your conditions, used to select values on range queries, and implement conditional put operations.

        Each entry key is the predicate type that will be used in Terrastore query string for range queries, e.g.:
        http://localhost:8080/bucket/range?predicate=type:expression&...
        And for put operations:
        http://localhost:8080/bucket/value?predicate=type:expression
    -->

    <util:map id="conditions">

        <entry key="jxpath">
            <bean class="terrastore.service.conditions.JXPathCondition"/>
        </entry>

    </util:map>

</beans>
